<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>waiter_web_application</title>
</head>

<body>
    <div class="container mainDiv">
        <div class="row">

            <div class="col-3 bg-light">
                <h3>col1</h3>
            </div>
            <div class="col-6 text-center display-5 w-100 p-3">
                <div class="row">
                    <h3 class="col-6 px-5">waiter_web_application</h3>
                </div>

                <div class="row py-5 px-5">
                    <form action="/daysObject" method="POST">
                    {{#each daysObj}}
                        <div class="form-check form-check-inline py-5">
                            <input class="form-check-input" type="checkbox" id="inlineCheckbox1" value="{{this.days}}">
                            <label class="form-check-label" for="inlineCheckbox1">{{this.days}}</label>
                        </div>
                        {{/each}}
                    </form>

                </div>

            </div>

            <div class="col-3 bg-light">
                <h3>col1</h3>
            </div>



        </div>

    </div>

</body>

</html>